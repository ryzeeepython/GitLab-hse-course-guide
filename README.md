# GitLab-hse-course-guide
Каждый новый раз: чек-лист из 1 минуты
1️⃣ Открыл ноутбук → открыл терминал

Ничего специального делать не нужно.

2️⃣ Перейти в папку с репозиторием
bash
Копировать код
cd ~/se2510-Stepan-Ryzhov-siryzhov

(или куда ты его клонировал)

3️⃣ Проверить, что SSH работает (по желанию, но полезно)
bash
Копировать код
ssh -T git@git.cs.hse.ru

Если видишь:

css
Копировать код
Welcome to GitLab, @siryzhov

— всё ок.

❗ Если попросит пароль от ключа — введи его (это нормально).

4️⃣ Обновить main

Перед новой задачей всегда начинай с чистого main:

bash
Копировать код
git checkout main
git pull

(даже если обычно ничего не меняется — это хорошая привычка)

5️⃣ Создать ветку под новую задачу
bash
Копировать код
git checkout -b submits/<task_name>

Пример:

bash
Копировать код
git checkout -b submits/palindrome

6️⃣ Решать задачу

пишешь код в tasks/<task_name>

запускаешь тесты

добиваешься зелёного результата

7️⃣ Сдать задачу
bash
Копировать код
git add tasks/<task_name>
git commit -m "Solve <task_name>"
git push -u origin submits/<task_name>

👉 На этом сдача завершена.

Если ты ставил пароль на SSH-ключ

Иногда после перезагрузки нужно один раз:

bash
Копировать код
ssh-add ~/.ssh/id_ed25519

Если не уверен — просто попробуй git push, он сам подскажет.

Если кратко, совсем кратко

Каждый раз ты делаешь одно и то же:

css
Копировать код
cd repo
git checkout main
git pull
git checkout -b submits/<task>
→ код → тесты
git commit
git push

Хочешь — сделаем это ещё проще

Могу:

написать тебе мини-шпаргалку на 5 команд (скопировать в заметки)

или помочь настроить CLion так, чтобы ты вообще почти не открывал терминал

Ты уже полностью “в бою”, дальше будет только быстрее 🚀
